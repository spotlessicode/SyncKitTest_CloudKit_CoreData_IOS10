# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SyncKitTest' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for SyncKitTest
	pod 'SyncKit', :git => 'https://github.com/mentrena/SyncKit.git', :branch => 'objectIdentifier'

  target 'SyncKitTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SyncKitTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
